# benchmarks/ — 路由 benchmark 冻结产物登记处

正式 routing benchmark 的 card 落盘处 + 索引。模板见
`.agent/templates/routing-benchmark-card.md`（仿 experiment-card 同构）。

规则：开跑前填 card 并在下表登记；freeze 字段（模型池 / policy_version / 预算上限 /
fixture 版本）运行期间锁定，不因中途 quota 变化切换。

| freeze_id | frozen_at | policy_version | question | card |
| --- | --- | --- | --- | --- |
| （暂无） | | | | |
