# .claude/rules/

<!-- template:begin -->

可选的附加 rule 片段（例如特定语言风格、特定工具约定）。

保持小而聚焦。跨 agent 的项目规则放 `AGENTS.md` / `.agent/`；这里只放 Claude Code 特有、且不适合塞进 thin `CLAUDE.md` 的补充规则。默认可为空。

<!-- template:end -->

<!-- 项目自定义区（template:end 之后，sync 不碰）：下游在此追加本项目特定内容；template:begin/end 块内是模板拥有的内容，如需改动请走 template-feedback 上报，勿在此直接改块内。 -->
