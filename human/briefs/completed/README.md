# human/briefs/completed/ — 已完成的 brief

从 `../active/` 完成并归档的 brief 放这里，作为历史记录。

- 命名沿用原文件名 `<YYYYMMDD>-<slug>.md`。
- 归档时可在文末补一行结果摘要与产物/handoff 指针（指向 `memory/handoffs/` 或 `deliverables/`）。
- **不要删改**已归档 brief——它们是任务曾经如何定义与验收的证据。

模板与字段说明见 `../active/README.md`。
