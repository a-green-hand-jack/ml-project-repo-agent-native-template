# Handoff 模板

给 fresh Claude Code session。保持 < 120 行。存 `memory/handoffs/<YYYYMMDD>-<slug>.md`。

```
Objective:
Current status:
Decisions:
Modified files:
Commands / results:
Open blockers:
Exact next prompt:
Forbidden paths:
```
