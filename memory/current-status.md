# current-status.md

> **活文件**。这是当前状态的单一真相源。每次 session 结束、compact 前、完成小目标时更新。
> fresh session 应能只读本文件 + `session-tree.md` 就接续工作。
> 下面是模板骨架，逐项填写，不要留空占位而不说明。

## 当前 objective

（一句话：这个 session 要达成什么。）

## Constraints

（时间 / 资源 / 禁改路径 / 必须遵守的 doctrine。禁改路径以 `session-tree.md` 的 Global forbidden paths 为准。）

## Files inspected

（读过哪些文件/路径，避免下次重复读。只列路径，不贴内容。）

## Files modified

（本 session 改动的文件 + 一句话说明。）

## Decisions

（关键决策 + 理由。重大决策应另立 ADR，见 `human/decisions/`。）

## Commands + results

（跑过的关键命令与结论。长输出只留结论/`grep`/`tail`，不贴全文。）

| command | 结论 |
| --- | --- |
|  |  |

## Subagent reports

（派生的 subagent 返回的 summary + 产物路径。不贴 subagent 全过程。）

## Open issues / blockers

（未解问题、卡点、待确认项。）

## Exact next steps

（下一位 agent 立即能执行的、有序的具体步骤。越具体越好。）

1.
2.

## Do-not-forget

（容易在 compact 后丢失的隐性约束、坑、口头约定。）
