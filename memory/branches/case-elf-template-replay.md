# Branch Status: case-elf-template-replay

> Generated by branch-reporter via read-only git inventory (`git log`,
> `git show --stat`, `git diff`, `git status`). No git write operations were
> performed to produce this file.

## Purpose

Standalone functional-test **case** branch for the
`ml-project-repo-agent-native-template` repo itself. It migrates the durable
research content of `~/Projects/ELF-template-case` — an instance of the older
`.harness`/`research-project-template` lineage, itself a stress-test case
built around the public `lillian039/ELF` PyTorch/JAX training project — into
this template's current `lab/` + `deliverables/` + `human/` shape, then is
meant to exercise (not fix) the template's validators, hooks, skills, and
subagents. Per this branch's own `PROJECT.md`: "这不是一个真正的研究项目，而是
`ml-project-repo-agent-native-template` 自身的功能测试 case。"

**This branch is intentionally not meant to merge back into `main`.** It is a
use-once-or-keep-long-term case/example branch, kept for human review of the
diff, not for integration.

## Parent session

Created via Claude Code's `EnterWorktree` tool from `main` at `fc18318`
("docs: explain why the template has no dedicated docs/ folder").

## Branch / base

- branch: `worktree-case+elf-template-replay`
- base: `main` @ `fc18318`
- committed history beyond base (2 commits):
  - `c164232` — "case: migrate ELF-template-case content into agent-native
    template shape"
  - `fdfa519` — "fix: migrate the two lab/code/tests files missed in the
    first migration pass"
- **working tree status: dirty at report time** — see "Live/in-progress
  working-tree state" below. This is real, apparently-concurrent, uncommitted
  work, not produced by this reporter.

## Worktree

- path: `.claude/worktrees/case+elf-template-replay/` (relative to repo root
  `/home/user/Projects/ml-project-repo-agent-native-template`)
- not pushed to any remote; no tracking branch configured.

## Linked issue / PR

None found. Not pushed to any remote; no issue/PR number is referenced in
either commit message or in this branch's `PROJECT.md`.

## Owned paths

Everything touched by the 2 committed commits (86 files changed in the
migration commit, 2 more in the fix commit), notably:

- `lab/code/**` (src, eval, experiments, configs, scripts, tests, pyproject,
  pre-commit config)
- `lab/infra/{launch,paths}/**`
- `lab/docs/**` (reference, research-narrative, audits, code/{dev,ops,
  outlines,src}, designs, experiments, timelines, updates, overview.md)
- `deliverables/paper/**` (bib, figures, macros, main.tex, sections, tables)
- `human/reviews/results/elf-case-smoke-result.md`
- `lab/research/{claims,evidence,experiment-ledger}.yaml`
- `PROJECT.md`, `.gitignore`
- `memory/current-status.md` (this branch's own living status doc)
- ANATOMY updates: `lab/ANATOMY.md`, `lab/code/ANATOMY.md`,
  `lab/infra/ANATOMY.md`

Plus, **currently uncommitted** (see below): further edits to
`lab/artifacts/{result,trace}-index.yaml`,
`lab/research/{claims,evidence,experiment-ledger}.yaml`,
`memory/current-status.md`, `lab/AGENTS.md`, `lab/README.md`,
`lab/code/AGENTS.md`, `lab/code/README.md`, and a new untracked
`lab/docs/README.md`.

## Forbidden paths

- Repo-wide hard boundary (same everywhere, per `.agent/action-boundary.md`):
  no editing/deleting bytes under `lab/data/**`, `lab/runs/**`,
  `lab/models/**` weights, `checkpoints/**`, `wandb/**`,
  `lab/infra/private/**`, `.env`.
- Branch-specific constraint (from this branch's own
  `memory/current-status.md` Constraints section): do not modify
  `~/Projects/ELF-template-case` — it is a read-only reference source outside
  this repo.
- No push to any remote unless a human explicitly asks (stated in this
  branch's `PROJECT.md` under "Remote / worktree 策略").
- CPU-only local execution; no GPU / EPFL cluster access from this
  environment (stated constraint, not a hard tool-level boundary).

## Anatomy / ledger impact

- `lab/ANATOMY.md`: +`docs/` leaf.
- `lab/code/ANATOMY.md`: +`eval/`, +`external/` (gitignored vendor-clone
  slot, not part of tracked history).
- `lab/infra/ANATOMY.md`: `launch/`, `paths/` documented as landed.
- `lab/research/{claims,evidence,experiment-ledger}.yaml`: populated (in the
  committed migration) with 4 claims migrated from the old repo's
  `memory/boards/claims.yaml`. Deliberately **re-graded**
  `status: supported` -> `status: partial` on all 4, because the supporting
  evidence is only log-grade (clone / py_compile / dependency import / one
  CPU-forward smoke) and the new template's evidence ladder
  (`lab/research/ANATOMY.md`) requires `>= metric` for `supported`. This
  re-grading is itself framed (in this branch's own notes) as a positive test
  finding: the new evidence-chain check is stricter than the old free-text
  `certainty` field it replaces.
- `risks.yaml`/`actions.yaml`/`provenance.yaml`/`source-visibility.yaml` from
  the old repo had no equivalent YAML slot in the new schema; migrated as
  prose into `lab/docs/research-narrative/project-board-risks-actions.md`
  and `lab/docs/reference/provenance.md` (not validator-checked, per this
  branch's own Decisions log).
- `memory/change-control.yaml` on this branch **still only has the
  template's example entry** — no `structure`-kind change was formally
  registered for this migration, even though it touched `ANATOMY.md` in 3
  places. Worth a human call on whether a test-case branch needs that
  formality or can skip it before any eventual push.
- **Currently uncommitted, in progress**: a further round of evidence/ledger/
  artifact-index registration (new `ev-elf-pytorch-runtime-smoke-replay-claude`
  evidence entry, `run-elf-pytorch-runtime-smoke-replay-claude` ledger entry,
  two new `lab/artifacts/result-index.yaml`/`trace-index.yaml` entries) plus
  anatomy/doc-sync edits to `lab/AGENTS.md`, `lab/README.md`,
  `lab/code/AGENTS.md`, `lab/code/README.md`, and a new `lab/docs/README.md`
  documenting the `docs/`/`eval/`/`external/` surfaces. See "Live/in-progress
  working-tree state" below — this is not yet reconciled or committed.

## Latest validation

- The last **committed** state of this branch's `memory/current-status.md`
  "Commands + results" table was empty, annotated "（迁移阶段尚未跑
  validator，见 Exact next steps）" — i.e. as of the migration-baseline
  commit, the repo's own governance validators had not yet been run against
  the migrated tree.
- However, the migration commit message (`c164232`) narratively claims: "All
  four governance validators pass clean (validate-governance,
  check-agent-harness, check-anatomy-drift, check-same-commit --staged)" and
  additionally describes a deliberate negative probe (temporarily flipping
  one claim back to `status: supported` with only log-grade evidence,
  confirming the overclaim check correctly rejects it, then reverting).
  **These two committed sources disagree** (commit message says
  validated-and-passing; the living status file said not-yet-run at the time
  it was committed) — an internal documentation inconsistency worth
  reconciling, not a cross-branch conflict.
- Separately, most of the "validation" prose already committed on this
  branch — `lab/docs/audits/complete-template-verification-report.md`,
  `lab/docs/audits/elf-template-case-report.md`,
  `lab/docs/audits/pytorch-elf-runtime-smoke-plan.md`,
  `human/reviews/results/elf-case-smoke-result.md` — is **historical content
  imported verbatim from the old `ELF-template-case` repo** (dated
  2026-07-08, referencing the old `research_project_harness` CLI and
  `/Users/jieke/...`/EPFL remote paths). It documents that *old* harness's
  validation of the *old* case repo, not this *new* template's validators
  being exercised against the migrated content. Do not read those files as
  evidence for this branch's own governance-validator status.
- **New since this reporter started (uncommitted, live):** the working tree
  now shows a real, fresh, local CPU-only replay actually having happened —
  `lab/code/external/ELF` (clone, `pytorch_elf` branch, commit
  `b29d8833609e9ab7f67cd9da39435ac5cea04837`) and
  `lab/code/external/.venv-elf-cpu` (disposable uv env) exist on disk
  (gitignored, confirmed present via `ls`), and
  `lab/research/evidence.yaml`/`experiment-ledger.yaml`/
  `lab/artifacts/{result,trace}-index.yaml` have uncommitted new entries
  describing: dependency install/import success (torch 2.13.0+cpu,
  transformers 4.44.2, datasets 2.19.1, einops, huggingface-hub, sacrebleu,
  rouge-score, wandb, muon-optimizer), a config load + override smoke, a tiny
  synthetic CPU forward pass reproducing the shapes from the old audit
  (`(2,4,8)` / `(2,4,32)`), and a `lab/code/` scaffold check
  (`compileall` exit 0, `pytest -q tests` → 2 passed). This is **not yet
  committed** and this reporter did not produce it — it appears to be
  concurrent work by another agent/subagent in this same shared worktree
  session (an artifact-librarian-style registration pass, alongside a
  repo-doc-steward-style anatomy/doc-sync pass touching
  `lab/{AGENTS,README}.md`, `lab/code/{AGENTS,README}.md`, and a new
  `lab/docs/README.md`).
- Still **not observed**: an actual fresh run of this repo's own
  `scripts/validate-governance.py` / `check-anatomy-drift.py` /
  `check-agent-harness.py` / `check-same-commit.py` against the current
  (dirty) tree, with recorded output. This reporter did not run them either
  (out of scope: read-only git inventory only). Recommend running these
  before committing the currently-uncommitted changes, to close the gap
  noted above and to validate the new anatomy/doc-sync edits.

## Merge target

**None — this is intentionally a standalone case/example branch, not meant
to merge into `main`.** Per this branch's own `PROJECT.md`: "不打算合回
main——它是一个用完即弃/长期保留的 case 分支，供人类 review diff。" A human
should decide whether to push it to a remote and/or keep it long-term for
review purposes; under the normal template workflow, no PR should be opened
against `main` for this branch.

## Sibling dependencies

- Depends one-way on `main` as its base (`fc18318`); nothing merges back the
  other way.
- No other active local or remote **branches**/worktrees exist in this repo
  at inventory time — no branch-level siblings, no path/ownership overlap,
  no circular dependencies to flag between branches.
- Within this single worktree, however, there is apparent **concurrent
  agent activity right now** (see "Live/in-progress working-tree state"):
  another subagent appears to be actively editing files in the same working
  tree as this reporter. Not a branch conflict, but a same-worktree
  concurrency note worth human awareness before anyone commits.

## Live/in-progress working-tree state (at report time)

`git status --porcelain` showed a **changing** set of modified/untracked
files across two checks taken a few minutes apart in this same reporting
pass — i.e. the working tree is a moving target, not a static snapshot.
Observed at last check:

```
 M lab/AGENTS.md
 M lab/README.md
 M lab/artifacts/result-index.yaml
 M lab/artifacts/trace-index.yaml
 M lab/code/AGENTS.md
 M lab/code/README.md
 M lab/research/claims.yaml
 M lab/research/evidence.yaml
 M lab/research/experiment-ledger.yaml
 M memory/current-status.md
?? lab/docs/README.md
?? memory/branches/case-elf-template-replay.md   (this report)
?? memory/worktree-status.md                      (this report)
```

None of the `M`/first-`??` entries above were produced by this reporter —
this reporter only ever ran read-only `git`/shell inspection commands plus
wrote its own two report files. Recommendation: do not treat this snapshot as
final; before committing, re-run `git status`/`git diff`, let any concurrent
agent finish, and have a human review the combined diff as a whole.

## Exit condition

Since there is no merge target, "exit" here does not mean "ready to merge."
Suggested exit conditions, drawn from this branch's own
`memory/current-status.md` "Exact next steps" (now partially, but not fully
or verifiably, addressed by the uncommitted work described above):

1. Let the currently-active concurrent work finish, then reconcile and
   actually run the 4 governance validators against the resulting tree;
   record the real result in `memory/current-status.md` (closing the gap
   noted above under "Latest validation").
2. Re-clone `lillian039/ELF` (`pytorch_elf` branch) and replay a CPU-only
   dependency-import + tiny-forward smoke — **appears already done**
   (uncommitted; verify and finalize rather than redo).
3. Register the smoke result via an artifact-librarian pass into
   `lab/artifacts/*-index.yaml` — **appears already done**
   (uncommitted; verify and finalize rather than redo).
4. Exercise remaining representative subagents/skills (experiment-orchestrator,
   checkpoint-writer, repo-doc-steward — partially evidenced above —,
   branch-reporter — this report —, test-runner + `worktree-pr-flow`/
   `experiment-workflow`/`artifact-indexing`/`anatomy-drift-control` skills)
   and confirm each behaves per its own `SKILL.md`/agent definition.
5. Run a few safe hook/permission probes (protected-path edit attempt,
   push-to-main attempt) and confirm they are blocked as expected.
6. Write up findings as a `lab/docs/audits/` report plus a
   `human/reviews/results/` summary, and update
   `memory/current-status.md` accordingly.
7. Once findings are written up and the working tree stabilizes, a human
   reviews the branch as a whole (diff review, not a merge review) and
   decides: keep it as a long-lived reference/example branch (do not merge
   or delete), and optionally push it to a remote for archival/sharing.

No autonomous agent action is required beyond continuing this documented
next-steps list; this branch does not need to reach a "mergeable" state
because merging was never the goal.

## Do-not-forget (carried over from this branch's own current-status.md)

- If this worktree is exited via Claude Code's `ExitWorktree` tool, use
  `action: keep` — test artifacts should be retained, not removed.
- `~/Projects/ELF-template-case` is a read-only reference source outside this
  repo; do not modify it. Any real execution/replay must stay local
  CPU-only (no EPFL/GPU access from this environment).
- Working tree was actively changing (concurrent agent) while this report was
  being compiled — re-verify state before acting on this file as if it were
  final/static.
